<?xml version="1.0" encoding="utf-8"?>
<!--
  Fixed width: always 4 launcher grid cells (see
  https://developer.android.com/develop/ui/views/appwidgets/layouts#anatomy_determining_size ).
  minWidth = (4 × 70dp) − 30dp = 250dp. Same value for min/max resize width so horizontal size
  cannot change; only height is resizable (resizeMode vertical).

  API 31+: targetCellWidth reinforces the default span; OPTION_APPWIDGET_SIZES width stays ~250dp
  while height still drives row count in LatestItemsAppWidgetProvider.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
  android:minWidth="250dp"
  android:minHeight="160dp"
  android:minResizeWidth="250dp"
  android:maxResizeWidth="250dp"
  android:minResizeHeight="90dp"
  android:targetCellWidth="4"
  android:targetCellHeight="2"
  android:updatePeriodMillis="1800000"
  android:initialLayout="@layout/widget_latest_items"
  android:configure="com.martijnmichel.directusexpo.app.LatestItemsWidgetConfigureActivity"
  android:resizeMode="vertical"
  android:widgetCategory="home_screen"
  android:description="@string/widget_latest_items_description" />
